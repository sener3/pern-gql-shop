query GetProductById($id: String!) {
    getProductById(id: $id) {
        id
        name
        price
        image
    }
}

query GetProducts {
    getProducts {
        id
        name
        price
    }
}
