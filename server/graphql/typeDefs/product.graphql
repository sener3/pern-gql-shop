type Product {
    id: String!
    name: String!
    price: Float!
    categoryId: String
}

input ProductInput {
    name: String!
    price: Float!
    categoryId: String
}

type Mutation {
    addProduct(product: ProductInput!): Product
}

type Query {
    getProducts: [Product!]!
}
